#include <iostream>

using namespace std;

int main() 
{
  
    int n, k;
    
    cin >> n;

    k = 1;

    int a[n][n]; 
    
    for (int i = 0; i < n; i++) 
    {
        for (int j = 0; j < n; j++) 
            cout << abs(j - i) << " ";

        cout << endl;
    }

  return 0;
}
