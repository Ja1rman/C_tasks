#include <iostream>

using namespace std;

int delete_duplicates(int* Array, int n)
{
    for (int i = 0; i < n; i++)
    {
        int p = 0;
        
        for (int j = 0; j < i; j++)
            if (Array[i] == Array[j])
                p++;
            
        if (p == 0)
            cout << Array[i] << " ";
    }
}

int main()
{
    int n;
    
    cin >> n;
    
    int* Array = new int[n];
    
    for (int i = 0; i < n; i++)
        cin >> Array[i];
   
    delete_duplicates(Array, n);
    
    delete [] Array;
    
    return 0;
}
