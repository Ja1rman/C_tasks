#include <iostream>
#include <cmath>

using namespace std;

int delete_annoying_primes(int* Array, int n)
{
    for (int i = 0; i < n; i++)
    {
        int cnt = 0;
        
        for (int k = 2; k <= sqrt(Array[i]); k++)
            if (Array[i] % k == 0)
                cnt++;
                
        if (cnt == 0 && Array[i] != 0)
        {
            int p = 0;
            
            for (int j = 0; j < n; j++)
                if (Array[i] == Array[j])
                    p++;
                
                
            if (p == 1)
                cout << Array[i] << " ";
                    
        }
        else
            cout << Array[i] << " ";
    }
}

int main()
{
    int n;
    
    cin >> n;
    
    int* Array = new int[n];
    
    for (int i = 0; i < n; i++)
        cin >> Array[i];
        
    delete_annoying_primes(Array, n);
    
    delete [] Array;
    
    return 0;
}
